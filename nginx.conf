server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        rewrite ^(/.+)\.html$ $1 permanent;
        try_files $uri $uri/ $uri.html /index.html;
    }
}
